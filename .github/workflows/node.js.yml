name: Node.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x, 14.x]

    steps:
    - name: deployment
      uses: appleboy/ssh-action@master
      with:
<<<<<<< HEAD
        host: 148.231.212.227
        username: bama
        password: reh@bilitacionCognitiv@20
=======
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
>>>>>>> Create devops workflow
        script: whoami
